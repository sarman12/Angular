import { SuccessPipePipe } from './success-pipe.pipe';

describe('SuccessPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SuccessPipePipe();
    expect(pipe).toBeTruthy();
  });
});
